Sources for the plugin `plugin.applovinMax.IronSource`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.applovinMax.IronSource" = {
            publisherId = "com.solar2d",
        },
    },
}
```
